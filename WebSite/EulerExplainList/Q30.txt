We can do some preliminary analysis here:  Since  \(7 * 9 ^ 5 = 413343 < 1000000 \) so value has to be lower than 1000000. Since it is sum so has to be higher than 10. Since \(3^5 > 100\) so if 2 digit it can only be 0, 1, 2 and \(2^5 = 32\) so 2 can't be included so it can't be two digit